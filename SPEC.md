# meetup-todo 仕様書

社内タスク管理用のシンプルな TODO アプリ。

## データ仕様

| フィールド | 型 | 説明 |
|---|---|---|
| id | string | 一意な ID |
| title | string | タスク名。必須・前後空白は除去・最大 100 文字 |
| dueDate | string \| null | 期限日 (YYYY-MM-DD)。任意 |
| completed | boolean | 完了フラグ |
| createdAt | string | 作成日時 (ISO 8601) |

## 機能仕様

### 1. 一覧表示

- 登録されたタスクを作成順(古い順)に表示する

### 2. タスク追加

- タスク名は必須。空文字・空白のみの場合は登録できず、エラーを返す (400)
- タスク名は前後の空白を除去して保存する
- タスク名が 100 文字を超える場合は登録できず、エラーを返す (400)
- 期限日は任意

### 3. 完了切替

- チェック操作のたびに、そのタスクの completed を **反転(トグル)** する
  - 未完了 → 完了、完了 → 未完了

### 4. 削除

- タスクを完全に削除する

### 5. フィルタ

- 「すべて」: 全タスクを表示する
- 「未完了」: completed が false のタスクのみ表示する
- 「完了済み」: completed が true のタスクのみ表示する

### 6. 期限切れ表示

- **期限日当日はまだ期限内であり、「期限切れ」として扱わない**
- 「期限切れ」となるのは、期限日を過ぎた日(期限日の翌日)以降である
- 期限切れ表示の対象は未完了タスクのみ。完了済みタスクには表示しない
- 期限日のないタスクは期限切れにならない

### 7. 優先度(次リリース予定・未実装)

- 各タスクに優先度 `high` / `medium` / `low` を持たせる
- 追加時に優先度を指定できる。未指定の場合は `medium` とする
- 一覧に優先度を表示する
- 「優先度順」の並び替えを選択できるようにする(high → medium → low、同一優先度内は作成順)
